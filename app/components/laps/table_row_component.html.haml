%tr.collapse{ id: dom_id }
  %td.text-muted.text-right{ colspan: 2 }
    %small
      %i= @lap.length_in_meters
      - if Rails.env.development?
        &nbsp;ID: #{@lap.id}
  %td.text-left{ colspan: 2 }
    %small
      #{@lap.to_timing} Δ
      &nbsp;
      %i.fa.fa-long-arrow-right
      &nbsp;
      #{@lap.timing_from_start} ⏱
