%tbody.table.table-sm{ id: dom_id }
  - @laps.includes(parent_association_name).by_distance.each do |lap|
    = render(Laps::TableRowComponent.new(lap: lap))
  = render(Laps::TableRowComponent.new(lap: closing_result_lap))
