%tbody.table.table-sm{ id: dom_id, class: @collapsed ? 'collapse' : '' }
  - @laps.each do |lap|
    = render(Laps::TableRowComponent.new(lap:))
  = render(Laps::TableRowComponent.new(lap: closing_result_lap))
