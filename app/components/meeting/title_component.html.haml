%h4.text-center.pb-3
  %i.fa.fa-map-marker
  = render(Meeting::LabelComponent.new(meeting: @meeting))
  - if @meeting.cancelled?
    .cancelled= t('activerecord.attributes.goggles_db/meeting.cancelled')
