%thead
  %tr
    %th.mevent-links{ colspan: 4 }
      - @meeting_events&.by_order&.each do |mevent_link|
        = link_to(mevent_link.event_type.label, "#mevent-#{mevent_link.id}",
                  data: { turbolinks: false })
      = link_to('#top-of-page', data: { turbolinks: false }) do
        %i.fa.fa-arrow-up
