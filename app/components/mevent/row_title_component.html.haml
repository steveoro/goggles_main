%thead.mevent-separator{ id: dom_id }
  %tr.text-center
    %th{ colspan: 4 }
      .mevent-title-content
        %h4.mb-0
          = @event_container.event_type.long_label
        - if @can_manage
          .mevent-report-missing
            = render(report_missing_button)
