.wrapper-body
  .spacer-left

  .main-content#top-of-page
    %section#my-past-meetings-title
      %h4.text-center= t('home.my.past_title')
      .text-center#back-to-dashboard
        = render(Title::UserDashboardLinkComponent.new(user: current_user))
    = render(partial: 'meeting_grid', locals: { grid: @grid, grid_filter_params: @grid_filter_params })

  .spacer-right
