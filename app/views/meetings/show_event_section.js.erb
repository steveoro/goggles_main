$('#loading-indicator').addClass('d-none')
newAnchor = "#mevent-<%= @meeting_event.id %>"
document.querySelector(newAnchor).outerHTML = `<%=
          escape_javascript(
            render(
              partial: 'show_event_section',
              locals: {
                meeting_event: @meeting_event,
                meeting_events: @meeting_events,
                event_index: @event_index,
                prgs_for_event: @prgs_for_event,
                managed_team_ids: @managed_team_ids,
                user_teams: @user_teams,
                current_swimmer_id: @current_swimmer_id,
                current_user_is_admin: @current_user_is_admin
              }) ) %>`;

// (NOTE: tooltips should be re-enabled by including the popover-controller as part of the rendered view)
location.hash = newAnchor
