- user_key = [user_signed_in?, current_user_is_admin, current_swimmer_id, managed_team_ids.to_a.sort, user_teams.to_a.map(&:id).sort]
- cache([cache_key_for_meeting(action: 'show_event_section', id: meeting_event.id, max_updated_at: @max_updated_at), local_assigns.fetch(:render_title, true), user_key]) do
  = render(Mevent::RowSeparatorComponent.new) if event_index.positive? && local_assigns.fetch(:render_title, true)
  = render(Mevent::RowTitleComponent.new(event_container: meeting_event, can_manage: user_signed_in?)) if local_assigns.fetch(:render_title, true)
  = render(Mevent::RowLinksComponent.new(meeting_events:))
  - prgs_for_event&.each do |mprg|
    = render(Mprg::RowSeparatorComponent.new)
    = render(Mprg::RowLinksComponent.new(meeting_programs: prgs_for_event))
    = render(Mprg::RowTitleComponent.new(meeting_program: mprg))
    - if mprg.relay?
      - mrrs = local_assigns[:mrr_rows_by_program]&.fetch(mprg.id, nil) || mprg.meeting_relay_results
      = render(MRR::TableComponent.new(mrrs:, managed_team_ids:, user_teams:, current_user_is_admin:))
    - else
      - mirs = local_assigns[:mir_rows_by_program]&.fetch(mprg.id, nil) || mprg.meeting_individual_results
      = render(MIR::TableComponent.new(mirs:, managed_team_ids:, current_swimmer_id:))
