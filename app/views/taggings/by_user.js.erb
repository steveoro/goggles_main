<% if flash[:error] %>
 alert('<%= escape_javascript(flash[:error]) %>' );
<% end %>
$("#user-star-<%= @meeting.id %>")
    .html('<%= escape_javascript(
            render(
                Grid::RowStarComponent.new(asset_row: @meeting, current_user: current_user, saved_ok: @saved_ok)
            )
          ) %>');
