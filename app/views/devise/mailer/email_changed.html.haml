- content_for(:template_subtitle) do
  = t('.greeting', recipient: @email)
  &nbsp;٩( ▀ ͜͞ʖ▀)۶
%p
  - if @resource.try(:unconfirmed_email?)
    = t('.message', email: @resource.unconfirmed_email)
  - else
    = t('.message', email: @resource.email)
